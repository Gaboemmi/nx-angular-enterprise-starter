# Core feature flags

Provider-agnostic, signal-first feature flags for Angular applications. The
library starts with a deterministic static provider and exposes an OpenFeature
adapter contract without bringing an external SDK into feature code.

Run `npx nx test core-feature-flags` to execute its unit tests.
