# Shared agent skills

This directory is reserved for reusable, tool-neutral repository skills.

Add a skill only after a repository convention is proven. Follow the procedure
and format in [`.ai/skills/README.md`](../.ai/skills/README.md). Tool adapters
may expose these skills, but the skills themselves must not depend on a
specific AI provider.
